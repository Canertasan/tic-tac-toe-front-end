{{#if isModalDialogVisible}}
  {{#modal-dialog targetAttachment="center"
                  translucentOverlay=true
                  onClose=(action "closeModalDialog")}}
    <p class="ember-modal-dialog-start-text"> Welcome to our tic-tac-toe game. First of all if you playing this game for the first time, you should add some user at Add User section on bottom left menu. If you have already users created before, you can go to playground directly and find user name from there. After selecting two users you can start your game. Have fun!</p>

    <button class="ember-modal-dialog-start-button" onclick={{action closeModalDialog}}>
      Let's Get Started
    </button>

  {{/modal-dialog}}
{{else}}
  <div class="flex justify-center my-10">
    {{#each this.users as |user| }}
      {{user.name}}
    {{/each}}
  </div>
  {{#if (eq this.users.length 2)}}
    <p>here</p>
  {{else}}
    <div class="flex justify-center my-10">
      <p class="mr-10"> If you created users before you can simply find in here </p>
      {{input class="shadow appearance-none border rounded text-gray-700 leading-tight focus:ring transform transition hover:scale-105 duration-300 ease-in-out mr-10"
        type="text"
        value=(mut this.currentUserName)
        focus-out="findUser"}}
      {{#if this.userFindError}}
        {{this.userFindError}}
      {{/if}}
    </div>
    <div class="flex justify-center">
      <p class="mr-20"> Or simply goes to add user page to add some :) </p>
      {{link-to 'Add User' 'user.new' classNames='text-black'}}
    </div>
  {{/if}}
{{/if}}

